# if we get a type mismatch error [string -> int etc.] for customer.freePasses,
# show this error instead of the default one
typeMismatch.customer.freePasses=Invalid Number

# this is related to the the Binding Result in the customerController